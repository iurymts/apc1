//Faça um programa em C que calcula a área de um triângulo (a = base x altura / 2). Considere base e altura números inteiros maiores que 0.

#include <stdio.h>

int main() {
  int base = 10;
  int altura = 1;

  int area = base * altura/2;

  printf("A area é %d\n", area);

  
  return 0;
}
